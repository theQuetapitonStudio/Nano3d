name: Discord Notify

on: [push]

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Send message to Discord
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
        run: |
          curl -H "Content-Type: application/json" \
          -d '{
            "content": "🚀 Novo update no repositório **${{ github.repository }}**\nAutor: **${{ github.actor }}**\nCommit: `${{ github.sha }}`"
          }' \
          $DISCORD_WEBHOOK
